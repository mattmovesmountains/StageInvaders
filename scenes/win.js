add([
  text('You killed out there'),
  pos(width()/2, height()/2 - 20),
  origin('center'),
])

add([
  text('..everyone from the looks of it.'),
  pos(width()/2, height()/2 + 20),
  origin('center'),
])